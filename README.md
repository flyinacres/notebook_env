# ?? Notebook Environment Lock: Batch Testing Harness`n`nA headless, CLI-driven testing framework for running generate_production_blueprint() across large corpora of Jupyter notebooks (.ipynb).`n`n---`n`n## ?? Quick Start (Windows CMD Setup)`n`nFollow these steps from standard Command Prompt (cmd.exe) after cloning this repository:`n`n### 1. Set Up the Python 3.13 Virtual Environment`npy -3.13 -m venv notebook_env`nnotebook_env\Scripts\activate.bat`npython -m pip install --upgrade pip`npip install ipykernel nbconvert papermill packaging`n`n### 2. Configure Git Tracking (.gitignore)`nEnsure your .gitignore file contains the following entries:`nnotebook_env/`n.venv/`nvenv/`n__pycache__/`n*.py[cod]`n.pytest_cache/`npinned_requirements.txt`n*.ipynb_checkpoints/`ntest_corpus/**/output_*.ipynb`n`n---`n`n## ?? Preserving Developer Setup on GitHub`n`nTo save your local developer environment:`npip freeze > requirements.txt`ngit add requirements.txt .gitignore`ngit commit -m "Save environment configuration and gitignore"`ngit push`n`nTo recreate the environment on a new machine:`ngit clone <your-repo-url>`ncd <your-repo-folder>`npy -3.13 -m venv notebook_env`nnotebook_env\Scripts\activate.bat`npip install -r requirements.txt`n`n---`n`n## ?? Headless Batch Execution`n`nUse batch_runner.py to execute tests headlessly across your test_corpus directory:`npython batch_runner.py
